{
  "id": "3",
  "title": "Ensure 'Allow Example Widget Above Lock' Is Set To 'Block'",
  "assessment_status": "Automated",
  "authoring_mode": "independent",
  "framework_mappings": [
    {
      "framework": "example",
      "framework_product": "example_windows",
      "framework_version": "1.0.0",
      "control_id": "1.3",
      "framework_level": ["Level 1"],
      "checked_date": "2026-08-20"
    }
  ],
  "policy_classification": {
    "control_surface": "device_config_profile",
    "platforms": ["windows_11"],
    "management_channels": ["intune_settings_catalog"]
  },
  "recommended_state": "Block",
  "description": "Illustrative example: a setting whose backing registry value lives under a per-provider key, so the provider's identifying value has to be looked up before the actual compliance-relevant value can be read.",
  "rationale": "Illustrative example rationale text - not real source prose.",
  "impact": "Illustrative example impact text - not real source prose.",
  "audit": {
    "methods": [
      {
        "method_name": "Registry Check",
        "type": "scripted",
        "description": "Illustrative example: first resolve the winning provider identifier, then read the setting stored under that provider's key.",
        "steps": [
          {
            "step_role": "lookup",
            "check_command": "$example_3_widget_winning_provider_guid = (Get-ItemProperty -Path 'HKLM:\\SOFTWARE\\Example\\PolicyManager\\current\\device\\AboveLock' -Name 'AllowExampleWidgetAboveLock_WinningProvider' -ErrorAction SilentlyContinue).AllowExampleWidgetAboveLock_WinningProvider",
            "check_command_verified": true,
            "output_description": "Illustrative example: the provider identifier that the second step substitutes into its own registry path.",
            "output_check": []
          },
          {
            "step_role": "compliance_check",
            "check_command": "$example_3_allow_widget_above_lock = (Get-ItemProperty -Path \"HKLM:\\SOFTWARE\\Example\\PolicyManager\\Providers\\$example_3_widget_winning_provider_guid\\Default\\Device\\AboveLock\" -Name 'AllowExampleWidgetAboveLock' -ErrorAction SilentlyContinue).AllowExampleWidgetAboveLock",
            "check_command_verified": false,
            "output_description": "Illustrative example output description for the compliance-relevant value.",
            "check_command_notes": "Illustrative example: this step substitutes the provider identifier captured in step 1 into its own registry path; check_command_verified is false because it is unconfirmed whether the captured identifier includes the enclosing braces the path segment expects, which affects whether the substituted path resolves correctly.",
            "output_check": [
              {
                "variable": "example_3_allow_widget_above_lock",
                "data_type": "integer",
                "operator": "eq",
                "value": 0,
                "value_source": "rule_defined"
              }
            ]
          }
        ]
      }
    ]
  },
  "remediation": {
    "methods": [
      {
        "method_name": "Settings Catalog",
        "type": "configuration_profile",
        "description": "Illustrative example remediation description.",
        "config_keys": [
          {
            "settings_catalog_path": "Above Lock\\Allow Example Widget Above Lock",
            "value": "Block"
          }
        ]
      }
    ]
  },
  "default_value": "Illustrative example default value text.",
  "references": [],
  "minimum_os_csp": "Illustrative example: Windows 10, Version 1809 and later.",
  "additional_information": null
}
